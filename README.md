Reproduce:
 1. Clone the repo `git clone https://github.com/g4s8/so59417246.git`
 2. Go into repo directory: `cd so59417246`
 3. Build Docker image: `docker build -t so59417246 .`
 4. Run the image: `docker run -p 8080:8080 so59417246:latest`
